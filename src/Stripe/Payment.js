import React from 'react';
import StripeContainer from '../Stripe/StripeContainer';
import './Stripe.css';

const Payment = () => {
  return (
    <div >
      <StripeContainer />
    </div>
  );
};

export default Payment;
